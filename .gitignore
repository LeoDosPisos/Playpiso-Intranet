node_modules

# Terraform — estado local e variáveis sensíveis
**/.terraform/
**/.terraform.lock.hcl
**/terraform.tfstate
**/terraform.tfstate.backup
**/tfplan
**/terraform.tfvars
**/backend.tf

# Python
__pycache__/
*.pyc
*.pyo

# .NET build outputs
**/bin/
**/obj/
# Slides de "projetos realizados" — assets pesados (alguns >100MB).
# Gerenciados fora do git (mantidos localmente / sincronizados por outro canal).
Backend/services/pptx-generator-service/slides/*/projetos/
